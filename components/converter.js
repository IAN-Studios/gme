/**
 * This file is the Converter Module, which allows dynamic real time conversion from a render object to an embed. Basically it allows custom creation of a "game", and alows it to be rendered as an
 * embed in discord.
 */
const Discord = require("discord.js");

module.exports = class converter {
    
    // No Constructor.

    /**
     * @description Converts a RenderFrame to a Discord.MessageEmbed Object.
     * @param {String} frame RenderFrame
     * @returns {Discord.MessageEmbed} Discord Embed Output
     */
    static async convertFrameToEmbed(frame) {

    }
}